# Add your project's Python dependencies here.
# For example:
# flask>=2.0
# numpy>=1.20
# pandas>=1.3
# scikit-learn>=0.24
Flask>=2.0
pandas>=1.3
# openai>=0.27 # LiteLLM will manage OpenAI, Azure, etc.
duckdb>=0.8.0
matplotlib>=3.0
python-dotenv>=0.15
pyarrow>=14.0.0
google-generativeai>=0.3.0 # For Gemini, if direct integration is chosen over LiteLLM for it
litellm>=1.15.0 # For unified LLM access
pytest-mock>=3.0 # For mocker fixture in tests
